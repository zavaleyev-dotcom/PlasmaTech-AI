import type { WorkspaceModule } from '../types';

export const scifinder = {
  id: 'scifinder',
  slug: 'scifinder',
  mode: 'live',
  example: 'AlTiSiN coating cutting tools',
} as const satisfies WorkspaceModule;
